<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <title>Daftar Matakuliah</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #ECEFCA;
            color: #213448;
            padding: 40px;
        }
        
        header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #94B4C1;
            padding-bottom: 10px;
        }

        header h1 {
            color: #213448;
            margin: 0;
            font-size: 2.5rem;
        }
        header p {
            color: #547792;
            margin-top: 8px;
            font-size: 1.2rem;
            font-weight: 600;
        }

        form {
            background-color: #94B4C1;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }

        form input, form button {
            padding: 10px;
            margin: 5px;
            border: none;
            border-radius: 5px;
        }

        form input {
            width: 120px;
        }

        form button {
            background-color: #213448;
            color: #fff;
            cursor: pointer;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
        }

        th, td {
            padding: 12px;
            border: 1px solid #ccc;
            text-align: center;
        }

        th {
            background-color: #547792;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .btn-delete {
            background-color: #c0392b;
            color: white;
            border: none;
            padding: 6px 10px;
            border-radius: 4px;
            cursor: pointer;
        }

        .btn-delete:hover {
            background-color: #e74c3c;
        }
    </style>
</head>
<body>

    <header>
        <h1>Daftar Matakuliah</h1>
        <p>Disusun oleh: Shafa Aulia - 122140062</p>
    </header>

    <form method="post" action="/matakuliah/tambah">
        <input type="text" name="kode_mk" placeholder="Kode MK" required>
        <input type="text" name="nama_mk" placeholder="Nama MK" required>
        <input type="number" name="sks" placeholder="SKS" required>
        <input type="number" name="semester" placeholder="Semester" required>
        <button type="submit">Tambah</button>
    </form>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Kode</th>
                <th>Nama</th>
                <th>SKS</th>
                <th>Semester</th>
                <th>Aksi</th>
            </tr>
        </thead>
        <tbody>
            {% for mk in daftar_matkul %}
            <tr>
                <td>{{ mk.id }}</td>
                <td>{{ mk.kode_mk }}</td>
                <td>{{ mk.nama_mk }}</td>
                <td>{{ mk.sks }}</td>
                <td>{{ mk.semester }}</td>
                <td>
                    <form method="post" action="/matakuliah/hapus/{{ mk.id }}" style="display:inline;">
                        <button class="btn-delete" type="submit" onclick="return confirm('Yakin ingin menghapus?')">Hapus</button>
                    </form>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

</body>
</html>
